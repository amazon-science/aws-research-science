---
description: Show live dashboard of experiments, GPUs, and running processes
---

Execute this command immediately without any preamble or explanation. Do not summarize or interpret the output - just run it and show the raw output:

```bash
python ${CLAUDE_PLUGIN_ROOT}/scripts/dashboard.py --once
```

After execution, say nothing else. The dashboard output speaks for itself.
