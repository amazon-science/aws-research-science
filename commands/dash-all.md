---
description: Show experiments from all sessions
---

Execute this command:

```bash
python ${CLAUDE_PLUGIN_ROOT}/scripts/dashboard.py --all
```

This shows experiments from all Claude Code sessions, not just the current one.
