---
description: Re-inject plugin context into current session after a plugin update
---

The plugin context has just been refreshed via hook. Acknowledge in one line that the DS plugin context is up to date, then stop.
